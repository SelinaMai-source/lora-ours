pyyaml>=6.0.1
numpy>=1.26.0
pandas>=2.2.0
torch>=2.2.0
matplotlib>=3.8.0

# 未来在算力机加载 Llama 3.1 8B 时使用（本地 debug 不强依赖这些 API）
transformers>=4.45.0
peft>=0.12.0
accelerate>=0.33.0

# 实验追踪（可选；仅在 output.tracking.use_wandb=true 时需要）
wandb>=0.17.0
